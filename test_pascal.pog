start {
    val n: Int = 5;
    var i: Int = 0;

    while (i < n) {
        var c: Int = 1;
        var k: Int = 0;

        while (k <= i) {
            println(c);
            c = c * (i - k) / (k + 1);
            k = k + 1;
        }

        i = i + 1;
    }
   // pog;
}
end
